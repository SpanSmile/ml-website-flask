apiVersion: v1
clusters:
- cluster:
    certificate-authority-data: # Certificate
    server: # IP ADRESS
  name: default
contexts:
- context:
    cluster: default
    user: default
  name: default
current-context: default
kind: Config
preferences: {}
users:
- name: default
  user:
    client-certificate-data: # Certificate
    client-key-data: # KEY
